[package]
name = "knipsa-ffi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "C-compatible FFI for the knipsa polygon clipping engine"
readme = "../../README.md"
documentation = "https://docs.rs/knipsa-ffi"
keywords = ["geometry", "polygon", "clipping", "ffi"]
categories = ["api-bindings", "graphics", "mathematics"]

[lib]
crate-type = ["cdylib", "rlib", "staticlib"]

[dependencies]
knipsa = { path = "../knipsa", version = "0.1.1" }

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true
